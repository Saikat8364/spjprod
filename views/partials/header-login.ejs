<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Suresh Paul and Jewellers Admin Login</title>
        <link rel="icon" href="/img/logo.png" type="image/png">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- Updated to Bootstrap 5 -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="css/styles.css">
        <!-- Font Enhancements -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <style>
            .header-main {
                background: linear-gradient(135deg, 
                rgba(80, 79, 79, 0.98) 0%,  /* Light gray base */
                rgba(90, 89, 89, 0.95) 100%), /* White overlay */
                url('/img/header-bg.jpg') center/cover;
                padding: 1.5rem 0;
                border-bottom: 3px solid #D4AF37;
                box-shadow: 0 4px 20px rgba(0,0,0,0.1);
                position: relative;
                overflow: hidden;
            }

            .header-main::after {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 50%;
                height: 100%;
                background: linear-gradient(
                    to right,
                    rgba(255,255,255,0) 0%,
                    rgba(212, 175, 55, 0.15) 50%,
                    rgba(255,255,255,0) 100%
                );
                animation: shimmer 5s infinite;
            }

            @keyframes shimmer {
                100% { left: 200%; }
            }

            .brand-container {
                display: flex;
                align-items: center;
                gap: 30px;
                margin-left: 8%; /* Shift content to the right */
                transform: translateX(3%); /* Fine-tune positioning */
            }

            .brand-logo {
                width: 130px;  /* Increased from 60px */
                height: 130px; /* Increased from 60px */
                background: url('/img/logo.png') center/contain no-repeat;
                filter: drop-shadow(0 2px 6px rgba(212, 175, 55, 0.3));
                transition: all 0.3s ease;
                transform-origin: center;
                /* Responsive Sizing */
                @media (max-width: 992px) {
                    width: 90px;
                    height: 90px;
                    
                }
                @media (max-width: 768px) {
                    width: 85px;
                    height: 85px;
                }
                @media (max-width: 576px) {
                    width: 90px;
                    height: 90px;
                }
            }

            .brand-text {
                color: #D4AF37;
                text-shadow: 0 2px 8px rgba(212, 175, 55, 0.3);
            }

            .brand-title {
                font-weight: 700;
                font-size: 2.5 rem;
                letter-spacing: 0.5px;
                margin-bottom: 0.25rem;
            }

            .brand-subtitle {
                font-weight: 500;
                font-size: 1rem;
                opacity: 0.9;
                letter-spacing: 0.2px;
            }
            .dashboard-header {
                background: linear-gradient(135deg, #DFD0B8, #948979),
                            url('/img/dashboard-bg.jpg') center/cover;
                padding: 2rem 0;
                border-bottom: 3px solid #D4AF37;
                margin-bottom: 2rem;
            }

            @media (max-width: 768px) {
                .brand-container {
                    margin-left: 0;
                    gap: 2.5%;
                    transform: translateX(0);
                    justify-content: flex-start;
                    text-align: center;
                }
                .brand-title {
                font-weight: 700;
                font-size: 1.8rem;
                letter-spacing: 0.5px;
                margin-bottom: 0.25rem;
            }

            }
        </style>
    </head>
    <body>
        <header class="header-main sticky-top">
            <div class="container-fluid">
                <div class="brand-container">
                    <a href="/" class="brand-logo-link">
                        <div class="brand-logo"></div>
                    </a>
                    <div class="brand-text">
                        <h1 class="brand-title mb-0">Suresh Paul & Jewellers</h1>
                        <p class="brand-subtitle mb-0">Administration Portal</p>
                    </div>
                </div>
            </div>
        </header>
        